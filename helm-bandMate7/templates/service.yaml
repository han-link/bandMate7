apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-bandMate7.fullname" . }}
  labels:
    {{- include "helm-bandMate7.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-bandMate7.selectorLabels" . | nindent 4 }}
